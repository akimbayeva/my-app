import React from 'react'
import { Panel, Label, PageHeader } from 'react-bootstrap'

const Header = () => (
    <PageHeader>
        <label>ToDo App</label>
    </PageHeader>
)

export default Header